/**
 * Scroll-reveal temporarily disabled — production freezes were linked to
 * IntersectionObserver + attribute writes during form focus / modal open.
 * Content stays fully visible; re-enable only after Contact is stable in prod.
 */
export function useReveal(_key?: string, _enabled = true) {
  /* no-op */
}
